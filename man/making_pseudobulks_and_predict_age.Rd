% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handling_Seurat_objects.R
\name{making_pseudobulks_and_predict_age}
\alias{making_pseudobulks_and_predict_age}
\title{Aggregate Seurat Data into Pseudobulks and Predict Cellular Age}
\usage{
making_pseudobulks_and_predict_age(
  seu,
  chunk_size = 1000,
  verbose = TRUE,
  REG = TRUE,
  Pasta = TRUE,
  CT46 = TRUE
)
}
\arguments{
\item{seu}{A Seurat object containing single-cell transcriptomic data.}

\item{chunk_size}{Numeric. The number of cells to aggregate into each pseudobulk sample. Default is \code{1000}.}

\item{verbose}{Logical. If \code{TRUE}, displays progress messages and summaries during processing. Default is \code{TRUE}.}

\item{REG}{Logical. If \code{TRUE}, include age predictions from the REG model. Default is \code{TRUE}.}

\item{Pasta}{Logical. If \code{TRUE}, include age predictions from the Pasta model. Default is \code{TRUE}.}

\item{CT46}{Logical. If \code{TRUE}, include age predictions from the CT46 model. Default is \code{TRUE}.}
}
\value{
A \code{data.table} containing age prediction scores along with metadata from the pseudobulk samples.
}
\description{
This function aggregates a Seurat object into pseudobulk samples using a specified chunk size and then predicts cellular age scores from the pseudobulk data. It combines the workflow of creating pseudobulk samples via \code{making_pseudobulks_from_seurat} and predicting age using \code{predicting_age_from_pseudobulks}.
}
\examples{
\dontrun{
# Assuming 'seu' is your Seurat object:
pdata <- making_pseudobulks_and_predict_age(seu, chunk_size = 1000)
head(pdata)
}

}
